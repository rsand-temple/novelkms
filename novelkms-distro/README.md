# NovelKMS - a Novel authoring Knowledge Management System

To run

```
C:\novelkms>java -jar novelkms.jar server C:\novelkms\config.yaml
```

Edit the config.yaml to change the location of your database files, logs, SSO provider settings, etc.